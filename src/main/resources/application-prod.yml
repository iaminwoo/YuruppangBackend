spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:postgresql://<render-db-host>:5432/<db-name>
    username: <db-username>
    password: <db-password>
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
