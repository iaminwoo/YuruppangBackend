spring:
  application:
    name: YuruppangBackend
  profiles:
    active: dev
    include: secret

  output:
    ansi:
      enabled: ALWAYS

  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    p6spy: info

decorator:
  datasource:
    p6spy:
      enable-logging: true